- content_for :breadcrumbs do
  %h1= @artist.name
  %p
    Estas aquí:
    = link_to "Admin", admin_path
    >
    = link_to "Artists", admin_artists_path
    >
    = @artist.name

#links
  = link_to "Back", admin_artists_path
  |
  = link_to "Edit", edit_admin_artist_path(@artist)
  |
  = link_to "New Painting", new_admin_artist_painting_path(@artist)
  

.info_artist.clearfix
  - if @artist.photo
    = image_tag @artist.photo.image.url(:thumb300), width: 200
  - else
    %img{:src => "http://placehold.it/200x200"}
  
  .bio
    %h2= @artist.name
    != RedCloth.new(@artist.bio).to_html
    
    %hr/
    
    = render partial: "admin/paintings/list" 