- num ||= 4

.photos
  - photos.in_groups_of(num, false) do |photos|
    .photos_row.clearfix 
      - photos.each do |p|
        = link_to image_tag(p.image.url(:thumb300), width: 150), p.image.url(:large), class: "gallery"